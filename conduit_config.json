{
  "output_folder": "",
  "always_run_handler": true,
  "log_level": 2,
  "handlers": {
    "redis_publish": {
      "name": "Redis Publisher",
      "description": "Publishes events to systems.events channel for database logging and subscribers",
      "command": "handlers/database_handler.py",
      "enabled": true
    },
    "example": {
      "name": "Example Handler",
      "description": "Template handler that logs events without side effects - use as reference",
      "command": "handlers/example_handler.py",
      "enabled": false
    }
  }
}